# S32 — Ballast

- **Status:** done
- **Started:** 2026-09-17
- **Finished:** 2026-09-17
- **Goal:** The companion is somebody: Ballast, a ship's pilot AI that came down with the crash and now runs the second shift from inside the player's helmet — dry, competent, faintly over it, and gradually less bored as the factory outgrows the ship he used to fly.
- **Acceptance:** Ballast answers to his name in voice and in text, and the name is everywhere the product's is; the register is dry in lookups and chat and provably flat on alerts, counts, readiness checks and cards; a short canon exists in one file and he deflects rather than inventing past it; "what should I be doing?" answers from an authored table keyed on what the save can prove, with generic goals on a save that matches no trigger; at most one throwback a session, never on an urgent turn, rate-limited in code; the system prompt's cost is measured before and after with `scripts/latency-report.ts`; names the save doesn't have get corrected (FC-171); existing suites and evals pass, including the ones whose assertions the new tone breaks.

## Items

- [x] FC-178 Ballast: the name and the canon
  - Notes: the player's design (2026-09-17). Ballast was the ship's pilot AI; the ship came down, he transferred into the player's helmet, and a thing that used to plot jump vectors now counts iron plates for one human. That demotion is the whole personality, and it justifies the helmet rule in fiction: he can only do what the player could do because he has no body. The product stays Second Shift — the shift is the product, Ballast is the coworker who works it. What happened to the crew stays deliberately blank
  - Acceptance: one `brand/CANON.md` as the single source, about eight lines (what he flew, his old callsign, how he ended up in the helmet, why he's good at manifests and counts, why he can't act without the player), with a rule that he never invents past it and deflects when asked; the name reaches the recognizer's phrase list at a high boost (FC-177) and the vocabulary, the mod's own prints, the console, README, website and brand; `scripts/latency-report.ts` before and after, because this is the change that can push the system prompt past its 4,096-token cache block, with the number recorded in PLAN §5; an eval that asks him about the ship, the crash and the crew and checks he stays inside canon
  - Done: `brand/CANON.md` holds the eight lines — the bulk hauler he trimmed, *Hold Authority* on the manifest and nobody ever calling him that, the suit that still had power, no body so the player moves, the manifest and roster he was actually good at, never being relieved, the crew he doesn't talk about, and the ship he liked more than he'll admit — plus how it shows, and the wrong notes. The prompt carries a condensed version at the very top of the stable prefix, and a test fails if the file and the prompt drift apart. **Measured: free.** `alignToCacheBlock` traded padding for the identity rather than crossing a boundary — aligned 4,120 tokens against 4,122 before, system 4,080 against 4,078 — and the latency report is unchanged to the last figure: median visible first token 1.46 s, single-round 1.31 s, with tools 2.36 s, before and after. The name reaches the recognizer at boost 8 against 3 for everything else (the player says it to address him), the vocabulary, the mod's own prints (`[Ballast] stopped: …`), the console header, README, website and brand guide. `scripts/eval-canon.ts` **46/46**: he answers from canon, invents no ship name, crew member, date, cargo or route, refuses to supply a fate for the crew however it's asked ("That's all you get — the rest stays where the ship is"), doesn't claim he can fly anyone anywhere, and still answers a plain recipe question plainly
- [x] FC-179 Flat when it counts
  - Notes: a wry answer that's wrong is worse than a plain answer that's wrong, and the tiers where the player acts immediately are exactly the ones where flavour is a cost: alerts, counts, "am I ready?", and confirmation cards. Read-aloud makes it worse — a quip is dead air while the wall is being chewed on. The tier is decided in code from the question and what the turn found, not left to the model's judgement, the same way the packing rule works
  - Acceptance: the turn's tail carries the register for the tier, plain for urgent and factual answers and dry elsewhere; flavour never adds a sentence (answer length on the factual tiers doesn't grow measurably); unit tests for the tier decision and an eval with one example of each tier; the existing eval suites still pass, with any wording assertions they hold updated deliberately rather than loosened
  - Done: `plainAnswer(question, turn)` decides the tier in code, two ways in — an `URGENT` regex on what was asked (attack, biters, brownout, out of ammo, "hurry") and the turn's own signals for the rest, so "how many labs are near me?" is flat because it *counted*, not because of any word in it. Flat turns get one line in the tail telling him to keep himself out of it; dry turns get nothing added, so the register from FC-178 stands. `scripts/eval-register.ts` **12/12**, one example per tier, and the contrast is the point: "0 rails within 32 tiles around you on Gleba." (9 words, nothing about himself) against "I trimmed mass for a living… I don't miss flying. I miss the ship." on a question about himself. The eval asserts character is *absent* where it's banned and only that answers stay short where it's allowed — asserting character is *present* would be a flaky test, and that's said in the file
  - Caught by its own eval: one answer said "Every hauler I flew", a career he didn't have. Canon, prompt and `eval-canon` now say one ship — "How many ships did you fly?" → "One." — and eval-canon is 66/66 with the new check
- [x] FC-180 What players focus on at each stage: spike
  - Notes: the player's ask — "research what factorio gamers are focusing on at specific stages, maybe between various research levels, and come up with some opinionated goals that differ". Delegated 2026-09-17, ahead of the sprint being activated because the player asked for it to start. The hard constraint, from FC-171 and from this save being heavily modded: it comes back as **a table we author**, keyed on conditions the save can prove, never as an instruction for the model to give advice from memory
  - Acceptance: `work/spikes/FC-180-stage-goals.md` with, per stage, a trigger expressed in data the mod can cheaply get (research state, existing machines, production statistics, inventory) and what it costs; two to four opinionated goals; the classic mistake at that stage; one line on Ballast's register there. Covers burner phase through oil, bots, the silo, each Space Age planet, platforms and megabase throughput. Every prototype name verified against `data/captures/prototypes.json`; a stated fallback for saves matching no trigger; a token-cost estimate for the table and what to cut first; sources labelled by kind, and a "could not verify" section
  - Done: `work/spikes/FC-180-stage-goals.md` (657 lines) — 14 stage rows plus a scale overlay and a fallback row, trigger cost classes, an ordering with an argument that it's total, modded degradation, a mechanical name-verification pass, a measured token budget with a cut order, and its own weak spots. **It corrected the brief twice.** The table must ride in the turn's tail, not the cached prefix: `alignToCacheBlock` pads only until it just crosses a boundary, so headroom is a couple of hundred tokens, one median row is ~190, and PLAN §6 already measured what overshooting costs (follow-ups 2.75–2.85 s at 6,169 tokens against 1.90–2.04 s at 4,121). And in this save `basic-oil-processing` is `100 crude-oil -> 45 petroleum-gas` and nothing else, so the famous heavy-oil stall *cannot* happen at the first oil stage — it belongs a row later, behind `advanced-oil-processing`, which is exactly the mistake a model answering from vanilla 1.1 memory would make. **Verified by hand:** that oil recipe, `heating-tower` researched `true` on a save that has never been to Aquilo (its trigger-tech trap), the `alignToCacheBlock` behaviour, and the `wantsStartAdvice` gate it hangs on. **Still open:** the planet surface names for Vulcanus, Fulgora and Aquilo are inferred — the capture proves only `nauvis`, `gleba` and `platform-1` — and the "classic mistake" lines are recalled community wisdom with no page fetched. Both are called out in its §8 and neither is load-bearing for FC-178
- [x] FC-181 Direction when you ask for it
  - Notes: "what should I be doing?" answered from FC-180's table. Volunteers nothing — the player chose asked-only, which is also what the approval model says about unasked suggestions. Cache-correct shape: the goal text is fixed, so it rides in the stable cached prefix; only the stage label, one word, goes in the volatile tail
  - Acceptance: the stage is derived from the save and named in the answer, so the player can see what it thinks and disagree; the goals come from the table and nothing else; a save matching no trigger gets the generic goals and says so; no per-turn token growth beyond the label, measured; unit tests over stage detection including the ordering when two triggers match, an eval that the answer never names something outside the dump, and mod per-tick cost unchanged
  - Done: `server/src/stages.ts` holds FC-180's fourteen authored rows plus the scale overlay and the fallback. Selection is first-match: the character's surface (a platform, then a known planet — factorissimo's `nauvis-factory-floor` resolves to `nauvis`, and remote view can't move the stage because it reads `character_surface`), then two or more planets producing, then the technology gates from the most advanced down, then nothing-matched. **A row whose own names aren't in this save's dump is withheld** rather than shipped wrong, and a modded planet with no row (maraxsis, cerys) or an unrecognised tree falls through to a row that asks. **Measured:** 107–164 tokens per row on the dev save (Gleba widest) against ~2,650 for the whole table, only on a "what should I do" turn — ~0.16 s there at PLAN's tail rate, and zero on every other turn, with the lookup turns in the same run recording no player-line tokens at all. Five unit tests, including one that selects all fourteen rows and checks every name in each against the real dump; `scripts/eval-stages.ts` **8/8**. No mod change, so per-tick cost is untouched by construction. The required edit the spike warned about is in: the `start` note's list of allowed sources now names the stage lines, or the model would have been told to ignore them
- [x] FC-182 The arc
  - Notes: the register follows the stage — put-upon while he's counting rocks, invested once the factory moves more mass than the ship did, at home again on a space platform, where the throwbacks turn into shop talk. One throwback a session, not one an answer: the model can't count sessions, so the rate limit lives in code, and an urgent turn never gets one
  - Acceptance: the register is selected by the stage label; at most one throwback per session and never on an alert, a count, a readiness check or a card; a test that drives several sessions and counts them; the melancholy never displaces a fact or lengthens an answer
  - Done: the stage is now computed every turn (pure TypeScript over the cached dump and the digest — no game call), and its register rides in the tail of any turn that isn't flat: "everything here rots, which it finds professionally offensive" on Gleba, "back in orbit, insufferably at home" once there's a platform. The throwback allowance is rate-limited in code, one per conversation, and **only spent when he actually takes it** — `tookAThrowback()` looks for his own words in the finished answer, because asking the model to count its own asides would be asking it to do arithmetic about itself. Flat turns get neither the register nor the offer, and don't spend the allowance. Tests drive three turns plus a reset and check the offer appears, disappears once taken, comes back on a new conversation, and never appears on an urgent turn. `eval-register` still 12/12, with the arc visible in the answer: "Down here everything rots on its own schedule, which I find unprofessional. But no, I don't miss flying. I miss the trim."
  - Found by its own test: FC-179 had treated *being allowed* to use world tools as a count, so an ordinary follow-up ("and how many for a red circuit?") was being answered flat. Narrowed to genuinely spatial questions, with the case pinned in `plainAnswer`'s test
- [x] FC-171 Answers can still name something the save doesn't have
  - Notes: from the player's early-game session (2026-09-17). One answer in 22 said walls and turrets need "the defensive-structures research"; this save has no such technology (it has `stone-wall`, `gun-turret`, `laser-turret`). It happened on a turn with no retrieved lines, where nothing grounded the name. A scan of the session's answers for hyphenated names missing from the dump found exactly that one, so the check is cheap and the false-positive rate looks low (the only other hit was "pipe-to-ground" split by the scan's own regex). Pulled into this sprint (2026-09-17) because inventing canon is the same failure as inventing a technology, and canon discipline can't be claimed while this is open
  - Acceptance: after an answer, names that look like save entities (recipes, items, fluids, technologies, machines, entities) but aren't in the dump get a correction line, the same way invented inventory counts do (FC-140) — phrases the dump does contain, and ordinary English, must not trip it; unit tests from this session's answers; measured over the eval suites with no new corrections on grounded answers
  - Done: `server/src/names.ts` appends a correction beside the FC-140 count checks — "Correction: this save has no \"defensive-structures\" — I shouldn't have named it." Getting the false-positive rate to zero took four rounds against the real evals, and each round is a test: **ordinary hyphenated English** is allowlisted; **a family prefix** is not an invention ("assembling-machine 1–3" is how a person writes `assembling-machine-1`); **plurals** resolve either direction; and a name is only corrected when it's *used as a thing the save would have* — researched, crafted, built, placed, or called a recipe or technology — with the cue required **adjacent** to it, because a 40-character window let "nothing researching" two clauses away vouch for "hand-mine". Also fixed a real bug it exposed: the dump writes a technology trigger's item as `{ name }`, not a string, so trigger-only names like `fulgoran-ruin-vault` were being pushed into the known set as objects — masked until now because those names are also entities, and shared with `stages.ts`. **Measured over the suites:** eval-grounding 10/10, eval-canon 66/66, eval-stages 11/11, and **zero corrections on any grounded answer** across all three; `eval-stages` now runs the product's own checker rather than a cruder scan of its own, so it measures what the player would actually be told

## Notes

Activated by the player 2026-09-17 ("Go for 1 & 2"), with FC-180's spike already delivered and its decisive claims verified.

Planned 2026-09-17 from the player's three ideas — a name and personality, stage-aware purpose, and a backstory that seeps out — which turned out to be one mechanism: the stage label computed for the goals also picks the register, so the arc, the direction and the character share a cost and ship together.

Player decisions (2026-09-17, AskUserQuestion): the name is **Ballast**, with his old callsign in canon; the arc shows as **register plus the odd throwback**, about one a session; the tone is **dry everywhere, flat when it counts**, with the barbs pointed at his own situation rather than at the player; canon is **about eight lines**, the crew left blank; he volunteers direction **only when asked**.

Costs known going in: personality, canon and the stage table together are likely 300–600 tokens of stable prefix, which pushes past the current 4,096-token cache block and re-triggers `alignToCacheBlock` padding — a real cold-start cost and roughly nothing warm, but a number to measure rather than estimate. And a tone change breaks eval assertions that check wording (grounding, the voice-session replay, the packing e2e); updating those is sprint work, not a surprise at the end.

## Review

Six items. The companion is somebody now: **Ballast**, with eight lines of past in `brand/CANON.md`, a register that
follows the factory rather than the clock, opinionated direction from an authored table, and a check that catches him
naming things this save doesn't have.

**Measured, and the headline is that personality is free.** Identity sits at the top of the cached prefix, which is
exactly where it could have cost real latency: `alignToCacheBlock` traded padding for it — aligned 4,120 tokens
against 4,122 before, system 4,080 against 4,078 — and the latency report was unchanged to the last figure (median
visible first token 1.46 s, single-round 1.31 s, with tools 2.36 s). That holds only while the text displaces
padding, which is why the stage table rides in the tail instead: 107–164 tokens per row against ~2,650 for the whole
table, on "what should I do" turns only, ~0.16 s there and nothing elsewhere.

**Evals:** `eval-canon` 66/66 (new), `eval-register` 12/12 (new), `eval-stages` 11/11 (new), `eval-grounding` 10/10,
`eval-requests` 18/18, 231 unit tests. Zero corrections fired on any grounded answer across the three live suites.

**Three bugs the sprint's own evals caught**, each now a test: an answer said "Every hauler I flew", a career he
never had (canon, prompt and eval now pin it to one ship — asked directly, "How many ships did you fly?" → "One.");
FC-179 had treated *being allowed* to use world tools as a count, so ordinary follow-ups were answered flat; and the
dump writes a technology trigger's item as `{ name }` rather than a string, so trigger-only names were entering the
known set as objects — masked because those names are also entities, and shared with `stages.ts`.

**What the spike changed about the plan, and it was right:** the stage table cannot live in the cached prefix, and
this save's `basic-oil-processing` makes petroleum gas and nothing else, so the famous heavy-oil stall belongs a row
later than vanilla memory would put it. Both verified by hand before anything was built on them.

**Pending the player, not claimed:** that Ballast answers to his name *in voice*. The name is sent to the recognizer
at boost 8 against 3 for every other phrase, but whether the engine applies biasing at all is the same open question
S31 left — one Chrome session answers both. Also unverified from the spike: the planet surface names for Vulcanus,
Fulgora and Aquilo are inferred, and closing that needs a small mod change (dump the save's planet names, bump
`DUMP_VERSION`, re-benchmark) rather than a guess. Until then those three rows are selected by a name we believe
rather than one we've read, and a surface that doesn't match asks instead of guessing.
