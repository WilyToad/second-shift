# S34 — Hands free, for real

- **Status:** active
- **Started:** 2026-09-18
- **Goal:** Hands-free that behaves like a coworker: you can talk over him, he stops, and what he says is honest to the last word — no restating the list, no borrowed material names, no stray markup — and the local-transcriber question gets its answer from your own recordings.
- **Acceptance:** With read-aloud on, speaking over an answer stops it within a second and becomes the next question, and Ballast's own voice never triggers it; a list is only restated when asked about; a one-word material this save doesn't have gets a correction and a turn of phrase never does; no HTML tag reaches the page; the words being spoken are marked in the thread as the voice reads them; FC-189's three-way comparison is written into PLAN with a verdict — including "keep the browser engine" if the numbers say so — measured on the player's own clips; every eval and the corpus test still green.

## Items

- [x] FC-226 A stray HTML tag reached the page
  - Notes: player's session (2026-09-18): an answer ended with a literal "</br>" line. The stream filter hides chart and tool-call blocks; it doesn't know HTML, and the thread renders text, so the tag showed as text
  - Acceptance: a bare HTML tag on its own line, or trailing an answer, is dropped in the stream filter at every token split; prose that mentions a tag in backticks is untouched; unit test
  - Done: the stream filter holds a line while it could still be nothing but a tag and drops it whole once it is, at every split; "x < y", "<->" and a tag in backticks pass through. Test uses the session's answer shape
- [x] FC-224 A one-word invented material slips past the name check
  - Notes: VERIFY row 18, live (2026-09-18): "Metal could be iron plate, copper plate, steel, aluminium, or something else". This save has no aluminium. FC-171 checks hyphenated names only, because that's how the save spells prototypes; a single ordinary-looking word the model borrows from other mods ("aluminium", "titanium", "tungsten" on a save without it) can't be told from English by shape
  - Acceptance: a short list of material and ore names common across Factorio mods is checked as whole words when they're offered as things the save might have ("could be X", "made of X", "X plate/ore"), and corrected only when the save has no prototype whose name contains the word; the FC-171 false-positive discipline applies — a turn of phrase is never corrected; unit test over this answer
  - Done: `materialCorrections()` over 37 usual suspects, three ways of being offered — a cue right before ("made of", "needs"), a unit right after ("plates", "ore"), or a list opened by "could be / such as / one of" — and only when no item, fluid, recipe or entity name in the save contains the word. Tests: the row-18 sentence corrected; holmium, tungsten and lithium (all present in some form) left alone; "aluminium foil" and "no aluminium here" left alone
- [~] FC-219 It restates the packing list on questions that aren't about it
  - Notes: player's session (2026-09-18): asked "Do you ever miss flying?" it answered in character, then added "The list is 0 of 4 done: 1 of 20 furnace, 0 of 250 belt…" unprompted. The active list rides in every turn's tail so the model can answer list questions, and it took that as an invitation to report on it
  - Acceptance: the list lines are answered from only when the question is about the list, readiness, stock or a build; a turn guidance line says so; unit test over this question; `e2e-packing` still passes
  - Built overnight (2026-09-18): a turn with a list up and a question that isn't about it (readiness, stock, bots, a build, list talk) gets "the list lines are context only: don't mention the list or its progress unless the question is about it"; agent test over the player's own sequence. **`e2e-packing` not yet re-run** — it needs the game, which stayed closed overnight on the player's battery; stays `[~]` until it has
- [ ] FC-217 Interrupt the voice by speaking
  - Notes: player, 2026-09-18: "if it is getting super verbal and I glance down and just read ahead, I'd like a way to interrupt the voice so I can speak again quickly." Escape stops speech and clicking Talk (or Alt+V) stops it and listens, but in hands-free mode the mic is deliberately muted while the answer is read so it doesn't hear the companion — so the natural interrupt, just talking, does nothing. The fix is barge-in: keep listening while speaking, and treat real speech as "stop reading, I'm talking", while ignoring the companion's own voice. FC-188's tap already hears everything; the hard part is not stopping on his own words
  - Acceptance: with read-aloud on and hands-free listening, the player speaking over the answer stops it within a second and their words become the next question; the companion's own voice never triggers it (measured on the ElevenLabs and browser voices); Escape and Alt+V keep working; unit tests for the decision, a session note for the false-stop rate
- [ ] FC-216 Show which words are being spoken
  - Notes: player, 2026-09-18: "when the answer is being read, can we have a progress-head or some type of visual reference of what words are being spoken?" Not urgent by their own account. The browser's `SpeechSynthesisUtterance` fires `boundary` events with a character index per word, which is enough for a moving highlight on the answer text; ElevenLabs streams audio per sentence, so there the head can only be per sentence unless its alignment data is used
  - Acceptance: while an answer is read aloud, the word (browser voice) or sentence (ElevenLabs) being spoken is marked in the thread and the mark moves with the voice; it never changes the text; off when read-aloud is off; no cost to the answer's first token
- [~] FC-189 Three transcribers over the same clips, before any of them ships
  - Notes: the decision this is for — local model or keep the browser engine — rests on FC-176's stop conditions, and both research passes agree the ordering hints from leaderboards don't transfer to 3–6 s clips of one voice over game audio. Deliberately a measurement, not a `/stt` route: no server endpoint, no supervision, nothing wired into a turn. Installs (`brew install whisper.cpp`, any pip environment, model downloads of ~0.6–1.6 GB) are the player's decision and happen only when they say so
  - Harness built overnight (2026-09-18), no installs: `scripts/compare-transcribers.ts` loads the FC-188 clips, scores the browser's transcript against the player's own wording (exact, word error rate, and the failure classes seen in their sessions — homophone, numeral, function word, dropped syllable — each tied to a real mis-hear in the tests), and runs whisper.cpp, mlx-whisper and parakeet-mlx only where they're installed, skipping with the install line otherwise. Clips without a correction are presumed right and say so. **Stays `[~]`: the three-way comparison itself waits on the player's word for the installs**
  - Acceptance: a script runs whisper large-v3-turbo (whisper.cpp or mlx-whisper), `parakeet-tdt-0.6b-v3` via parakeet-mlx, and Moonshine over the FC-188 clips, alongside the browser transcript already recorded, and reports per clip and in aggregate: exact match against what the player said, word error rate, and each of the three failure classes seen so far (near-homophone "wire/wine/where", numeral formatting "Got10", function words "of there"); run with and without the 555-word vocabulary as prompt or hotwords wherever the model supports it, so biasing is measured rather than assumed — `parakeet-mlx` has none (listed as *Todo*), and Qwen3-ASR's context biasing is an unverified claim worth half an hour before it's ruled in or out; added delay p50 and p95 with the model resident, against today's ~1.5 s to first token; hallucination counted on short and near-silent clips, because that's the failure that doesn't look like one; the verdict written into PLAN §5 either way, including "keep the browser engine" if the numbers say so

## Notes

Planned 2026-09-18 at the end of the live-testing session, from what it left behind: three honesty gaps (FC-219, FC-224, FC-226) found by the player hearing and reading answers, two hands-free requests in their own words (FC-216, FC-217), and the transcriber comparison that the session's nineteen clips finally made possible (FC-189). Future items stay in the backlog by the player's choice.

Order: the three tech-debt items first — two need no game, all three are small — then barge-in (FC-217), which is the change the player will feel most, then the spoken-word mark (FC-216), and FC-189 last because its installs (`brew install whisper.cpp`, a pip environment, 0.6–1.6 GB of models) wait on the player's word and its verdict stands on its own.

Risks named going in: barge-in's false-stop rate is the whole item — a companion that stops mid-sentence on its own voice is worse than one you can't interrupt, so it ships behind a measurement, not a hunch. FC-189 measures on clips from one voice and one evening; the verdict says so.
